CREATE TABLE SESSAOUSUARIO (
    ID        INTEGER PRIMARY KEY,
    IDUSUARIO INTEGER NOT NULL
                      REFERENCES USUARIO (ID),
    IDMAPA    INTEGER REFERENCES MAPAS (ID) 
                      NOT NULL
);
